import java.util.ArrayList;

// Dynamic array - List and ArrayList
ArrayList<Integer> list = new ArrayList<>();
list.add(10); // Add
int val = list.remove(list.size() - 1); // Remove

// Slicing
String[] strs = {"hello"};
String newString = strs[0].substring(0, 1);
