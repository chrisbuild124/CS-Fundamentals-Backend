import java.util.Set;
import java.util.HashSet;

Set<Integer> set1 = new HashSet<>(); // Usable to multiple DSA types
set1.add(1);        // insert
set1.remove(1);     // delete
set1.contains(1);   // lookup
set1.size();        // size

HashSet<Integer> set2 = new HashSet<>();
set2.add(1);        // insert
set2.remove(1);     // delete
set2.contains(1);   // lookup
set2.size();        // size
